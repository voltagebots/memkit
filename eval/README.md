# memkit-eval

Private evaluation harness: MemKit vs baselines, real measured comparison.

Not published. See `plan.md` for the full design, including the privacy
architecture (`data/private/` never leaves this machine, only a scrubbed
`RESULTS_PUBLIC.md` is ever manually copied to techbots-dev).
